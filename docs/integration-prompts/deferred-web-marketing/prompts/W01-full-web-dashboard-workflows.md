# W01 Full Web Dashboard Workflows

Status: Deferred.

Do not run unless the user explicitly switches from mobile/backend work to web dashboard product work.

## Purpose

Build real `apps/web` dashboard workflows for desktop operations after product scope exists.

## Deferred Because

- The mobile app remains the MVP product.
- `apps/web` is scaffolded only.
- Dashboard routes, permissions, data contracts, and UX scope are not approved yet.

## Not In Scope

- B09 live provider QA.
- Marketing site work.
- Production deployment config.
- Signup/waitlist integrations.

## Future Prompt Requirements

When this becomes runnable, the prompt must specify:

- Exact dashboard workflows.
- Auth and role requirements.
- API contracts.
- Data privacy rules.
- Mobile/web boundary rules.
- Verification commands.

## Suggested Commit Message

`add web dashboard workflows`
