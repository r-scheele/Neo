# Local-Only Placeholder Report

Date: 2026-05-27

This report lists every local-only, mock, fake, placeholder, or dev-only behavior found during the MVP audit. It does not prescribe code changes beyond future integration passes.

## Global Local-Only Mechanisms

| Mechanism | Files | Current behavior | Risk | Required for MVP | Recommended pass |
| --- | --- | --- | --- | --- | --- |
| Mock screen state query params | `components/feedback/ScreenState.tsx`, `app/(tabs)/*.tsx`, `app/conversation/[id].tsx`, `app/order/*.tsx`, `app/receipt/[id].tsx`, `app/customer/[id].tsx`, `lib/mocks/localPreview.ts` | `?state=loading`, `empty`, `error`, `offline`, or `permission` forces dev/demo states only when local preview mode is enabled in development. Non-development builds ignore these params and render ready state. | Low | No, not as public route params | Keep documented in `docs/local-preview-controls.md`; remove only when real backend state QA replaces it. |
| Mock role query params | `app/(tabs)/approvals.tsx`, `app/receipt/[id].tsx`, `app/modals/permission.tsx`, `features/permissions/permissionData.ts`, `lib/mocks/localPreview.ts` | `?role=staff`, `manager`, or `owner` changes role gates only in development local preview. Non-development builds ignore mock roles and fall back to the safest visual role, `staff`; backend-backed sensitive writes use trusted membership roles. | Medium | No | Keep mock roles local-only visual preview; verify real owner/manager/staff memberships through backend QA. |
| Typed fixture/demo data | `features/*/*Data.ts`, selected screen files | B05 now gives Today, Orders, Receipts, Follow-ups, and Customers backend API paths with isolated demo fallback for old fixture IDs. Inbox, Conversation, Approvals, setup sub-records, and some local action notices remain fixture/local. | Medium | No, not for production data | B07-B08 plus remaining feature prompts. |
| Local success timers | `features/conversation/ConversationDetailScreen.tsx`, `features/order/CreateOrderScreen.tsx`, `features/approvals/ApprovalQueueScreen.tsx`, `features/today/TodayCommandCenterScreen.tsx`, `features/inbox/InboxConversationListScreen.tsx`, `features/setup/WhatsAppSetupScreen.tsx` | `setTimeout` simulates loading, retry, sending, refreshing, or saving. | Medium | No | Backend/API and state management. |
| Placeholder screen component | `components/layout/PlaceholderScreen.tsx` | Generic local mock screen state component remains in source. | Low | No, if unused | Release cleanup after route audit. |
| Integration prompt status | `docs/integration-prompts/integration-prompt-index.md` | Phase A client prompts 01-12 have now been run; Prompt 08 completed local-only state hardening after Prompts 10-12 were run out of order; B01-B06 Supabase/API/schema/auth/commerce/permissions sync is complete for current sensitive endpoints. | Low | No | Do not run B07-B08 until dependencies are ready. |

## Feature-Level Placeholder Inventory

| Feature/screen | Files involved | Current status | Real behavior | Local-only/fake behavior | Missing dependency/service | Missing env vars | MVP required | Risk | Recommended integration pass | Suggested commit message after fixing |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Welcome | `features/welcome/WelcomeScreen.tsx` | Partial | Static entry UI and navigation; auth route layout redirects signed-in users. | Live signed-in redirect QA requires a real local Clerk publishable key and test account. | Future backend for business ownership. | `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY`. | Yes | Medium | Persistence/backend boundary. | `complete safe local persistence` |
| Register / Sign In | `features/auth/RegisterSignInScreen.tsx` | Integrated auth UI | Email/password sign-in and sign-up create Clerk sessions; sign-up supports email verification code; signed-in users redirect by setup status. | Live auth cannot run without a real local Clerk publishable key and configured test account. | Future backend for business ownership. | `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY`. | Yes | Medium | Persistence/backend boundary. | `complete safe local persistence` |
| Setup Checklist | `features/setup/SetupChecklistScreen.tsx`, `stores/useSetupStore.ts` | Partial/local prototype | Setup progress reads from Zustand. | Default state starts mostly complete; no authenticated business ownership. | Clerk, future backend. | Clerk key. | Yes | High | Auth and persistence cleanup. | `normalize setup progress state` |
| Business Profile | `features/setup/BusinessProfileScreen.tsx` | Partial | Saves draft locally to setup store. | No business API record or account ownership. | Clerk, future backend. | Clerk key. | Yes | Medium | Auth, backend boundary. | `connect business profile ownership` |
| Business Type | `features/setup/BusinessTypeScreen.tsx` | Partial | Saves selection to setup store. | No backend business defaults. | Future backend. | None in client. | Yes | Medium | Persistence/backend boundary. | `finalize business type persistence` |
| WhatsApp Setup | `features/setup/WhatsAppSetupScreen.tsx` | Local-only | User can view/retry a safe setup status. | Mock connected/disconnected status; manual number entry disabled; no WhatsApp validation. | Backend/API, WhatsApp Cloud API server setup. | None in client. | Yes | High | Backend/API boundary. | `connect whatsapp setup status` |
| AI Personality | `features/setup/AiPersonalityScreen.tsx` | Partial | Saves safe local AI preference settings. | Preview is generated locally; no AI policy/backend runtime. | Backend AI orchestration later. | None in client. | Yes | Medium | Backend/API boundary. | `connect ai personality policy` |
| Payment Rules | `features/setup/PaymentReceiptRulesScreen.tsx` | Partial/local-only | Enforces manual receipt review and local setup preferences. | Payment methods are local preferences; no payment provider or bank verification. | Backend/payment boundary. | None in client. | Yes | High | Backend/API boundary. | `connect payment rules boundary` |
| Delivery Zones | `features/setup/DeliveryZonesScreen.tsx` | Partial/local-only | Local validation and list editing. | Only count is persisted; zone records are not saved globally or synced. | Backend later. | None in client. | Yes | Medium | Persistence/backend boundary. | `connect delivery zone storage` |
| Product Basics | `features/setup/ProductBasicsScreen.tsx` | Partial/local-only | Local product entry and validation. | Product records are screen-local; only count is persisted. | Backend/product catalog later. | None in client. | Yes | Medium | Persistence/backend boundary. | `connect product basics storage` |
| Today Command Center | `features/today/TodayCommandCenterScreen.tsx`, `features/today/todayCommandData.ts`, `stores/useOperationsStore.ts`, `stores/useConnectivityStore.ts` | Backend-backed for commerce counts | Operational UI, states, analytics, safe shared attention count, backend Today commerce counts, and shared last-synced UI metadata. | Connectivity remains local UI state; urgent chats remain `0` until WhatsApp integration. | WhatsApp workflow and real connectivity source. | Public API base URL outside git. | Yes | Medium | WhatsApp workflow later. | `connect whatsapp workflow` |
| Inbox | `features/inbox/InboxConversationListScreen.tsx`, `features/inbox/inboxConversationData.ts` | Local-only | Search/filter UI and route links. | Conversation list is fixture data; no WhatsApp sync. | Backend/API, WhatsApp sync. | None in client. | Yes | High | Backend/API. | `connect inbox conversations` |
| Conversation Detail | `features/conversation/ConversationDetailScreen.tsx` | Local-only | Chat UI, draft editor, local validation, route links. | "Send" only shows local feedback; attachments and manual send are disabled; messages and draft are fixtures; draft text remains component-local and is not stored in Zustand. | Backend/API, WhatsApp send, AI generation, approved draft-state decision. | None in client. | Yes | High | Backend/API and draft state. | `connect conversation workflow` |
| Create Order | `features/order/CreateOrderScreen.tsx`, `features/order/createOrderForm.ts` | Backend-backed for B06 | Controlled form, totals, submit state, backend order creation, server permission check, and audit write. | Old fixture/demo IDs remain isolated fallback. | Transaction-safe audit/mutation RPC hardening before launch. | Public API base URL outside git. | Yes | Medium | Launch hardening. | `enforce server permissions` |
| Order Detail | `features/order/OrderDetailScreen.tsx`, `features/order/orderDetailData.ts` | Backend-backed for B06 | Detailed order UI loads backend order records by durable ID; backend cancel and delivery status actions use server permission/audit. | Missing/local fixture IDs still show isolated demo fallback; payment reminder still does not send WhatsApp. | WhatsApp send and transaction-safe audit/mutation RPC hardening. | Public API base URL outside git. | Yes | Medium | B07, launch hardening. | `connect whatsapp workflow` |
| AI Approval Queue | `features/approvals/ApprovalQueueScreen.tsx`, `features/approvals/approvalQueueData.ts` | Mixed | Queue UI, visual demo gates, analytics, and backend decision path for durable approval IDs. | Fixture approvals remain local; real AI draft generation is not implemented. | AI backend, live approval records. | Public API base URL outside git for durable approvals. | Yes | High | B08 AI workflow. | `connect approval decisions` |
| Receipt Review | `features/receipts/ReceiptReviewScreen.tsx`, `features/receipts/receiptReviewData.ts` | Backend-backed for B06 | Trust-first review UI loads backend receipt records and saves human review decisions through server permission/audit. | No receipt upload, OCR, bank lookup, customer message, staff workflow, or payment-provider verification yet. | Media/OCR/payment verification and transaction-safe audit/mutation RPC hardening. | Public API base URL outside git. | Yes | High | Later media/OCR/payment work. | `connect receipt media workflow` |
| Follow-ups | `features/follow-ups/FollowUpsScreen.tsx`, `features/follow-ups/followUpQueueData.ts` | Backend-backed for B06 | Follow-up queue loads backend records; complete/reschedule persist through server permission/audit. | No WhatsApp message is sent; local fixture IDs remain isolated fallback. | WhatsApp send. | Public API base URL outside git. | Yes | Medium | B07. | `connect whatsapp workflow` |
| Customer Profile | `features/customer/CustomerProfileScreen.tsx`, `features/customer/customerProfileData.ts` | Backend-backed for B05 | Customer profile loads safe backend metrics, order history, preferences, and notes summary. | Note editing and create follow-up action remain local/not connected; no customer-write endpoint exists yet for server permission/audit enforcement. | Customer-write permission/audit contract and follow-up creation contract. | Public API base URL outside git. | Yes | Medium | Customer workflow later. | `connect customer workflow` |
| Settings | `features/settings/SettingsScreen.tsx`, `features/settings/settingsData.ts`, `stores/useUserPreferencesStore.ts`, `stores/localStateReset.ts` | Partial | Safe preference toggles persist; sign-out calls Clerk and clears safe local user state. | Business/team/security rows are not connected; support/help not connected. | Future settings APIs with server permissions/audit. | Clerk key; PostHog key for analytics pass. | Yes | High | Settings backend later. | `connect settings workflow` |
| Permission Denied | `features/permissions/PermissionDeniedScreen.tsx`, `features/permissions/permissionData.ts` | Mixed | Clear permission explanation, local analytics, and real denied-write routing from API responses. | Ask-owner action is local; mock route roles remain dev-only visual preview. | Team invite/ask-owner workflow. | Clerk key if Clerk role metadata is used. | Yes | Medium | Team workflow later. | `connect permission gates` |

## Explicit Safe Local-Only Copy Found

The code correctly warns users in many places that the current action is local-only. Examples include:

- "No WhatsApp message was sent" in conversation and follow-up flows.
- Backend receipt decisions now save through the API for durable IDs; local receipt fixtures still warn that no payment verification or message is sent.
- "Decision saved to local UI only" in approvals.
- "No bank lookup or payment provider was contacted" in backend receipt review decisions, with local mock copy isolated to fixture paths.
- Local permission preview copy remains only for demo/visual gates; backend denied-write responses now route to the permission UI.

These warnings should remain until the real integration works end to end. After integration, they should either be removed or isolated behind explicit dev/demo mode.
