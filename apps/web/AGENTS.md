# Web Dashboard Instructions

This workspace contains the future `app.neo.com` dashboard.

- Treat this as scaffolded future product surface unless a prompt explicitly scopes web dashboard work.
- Do not duplicate mobile MVP workflows here without product approval.
- Do not implement fake backend behavior.
- Future authenticated routes must use Clerk and call Supabase Edge Functions through the approved API boundary.
- Use only public `NEXT_PUBLIC_*` variables in client-rendered code.
- Never put server secrets, Supabase service role keys, WhatsApp tokens, AI keys, payment secrets, webhook secrets, direct database URLs, or admin credentials in this app.
