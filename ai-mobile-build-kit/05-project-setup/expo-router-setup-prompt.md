# Expo Router Setup Prompt

Copy, fill, and send this to a coding agent when setting up Expo Router.

```md
Anchor:
Read AGENTS.md first if it exists and follow it strictly.

Task:
Set up Expo Router for this Expo React Native TypeScript project.

Constraints:
- Keep changes focused on routing setup.
- Do not implement product features.
- Do not redesign existing screens.
- Do not install unrelated libraries.
- If required packages are missing, list the exact install command and wait for approval.
- Preserve existing code unless route setup requires moving entry files.

Reference:
- Current Expo Router documentation: [PASTE_DOCS_OR_LINK]
- Desired route groups: [PUBLIC/AUTH/TABS/MODALS]
- Screen map: [PASTE_OR_LINK_SCREEN_MAP]

Acceptance criteria:
- Expo Router is configured.
- Root layout exists.
- Requested route groups exist.
- App launches without route errors.
- Navigation between starter routes works.
- Explain files changed and how to test.
```

## Done Looks Like

Routing is ready when the app opens through Expo Router and the planned route groups exist without feature implementation.
